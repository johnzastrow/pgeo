# Copy to secrets.env (chmod 600). Never commit secrets.env.
# Read only by scripts/render_config.sh, never by docker compose or the pelias CLI.
# Your own batch.openaddresses.io token. Leave empty to use the importer's shared default.
OA_TOKEN=
